services:
  redis:
    image: redis:7-alpine
    container_name: llm-interceptor-redis
    command: redis-server --requirepass mypassword
    ports:
      - "6379:6379"
    volumes:
      - /Users/nightfield/self/data/redis:/data
